# ==============================================================================
# Federated Learning Project - Step 1 Core Baseline Requirements
# ==============================================================================

# 核心深度學習框架 (核心計算、LeNet 模型與張量操作)
torch>=2.0.0

# 電腦視覺與資料集預處理 (MNIST 下載、影像等比例縮放 Resize、張量轉換)
torchvision>=0.15.0
Pillow>=9.0.0

# 數值計算與矩陣處理 (Non-IID 資料標籤隨機分配與索引切片)
numpy>=1.22.0

# 書面報告核心：基準實驗結果視覺化 (實驗一與實驗二折線圖、長條圖繪製)
matplotlib>=3.5.0